apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: {{ template "webhook-demo.name" . }}-certs
  labels:
    app: {{ template "webhook-demo.name" . }}
    chart: {{ template "webhook-demo.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  annotations:
    "helm.sh/hook": "pre-install"
    "helm.sh/hook-delete-policy": "before-hook-creation"
data:
{{ ( include "webhook-demo-certs" . ) | indent 2 }}